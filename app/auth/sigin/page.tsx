import { Button } from "@mui/material";
import React from "react";

const Page = () => {
  return (
    <div>
      <Button variant="contained" className="bg-black">
        Contained
      </Button>
    </div>
  );
};

export default Page;
